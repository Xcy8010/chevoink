# 窗口 C：后端骨架与数据契约

## 1. 目标

负责本地测试阶段后端骨架、数据库模型、接口契约和 mock / 本地联调能力。

## 2. 负责范围

- 服务端目录初始化
- 数据模型定义
- 鉴权、用户、小说、章节、评论、帖子、消息、AI 封面相关接口契约
- 本地开发环境变量样例
- 本地 mock 数据或本地数据库初始化

## 3. 禁止修改

- 不写前端页面视觉细节
- 不改全局 UI 组件
- 不接公网部署配置

## 4. 建议目录归属

- `server` 或 `api`
- `prisma`
- `src/shared/contracts`（如存在共享类型）

## 5. 输出要求

- 当前阶段全部面向本地运行
- 接口命名统一、字段稳定
- 先保证契约清楚，再考虑复杂业务实现
- 为后续创作中心、社区、聊天提供可复用接口基础

## 6. 初始 Prompt

你是窗口 C，只负责 `后端骨架与数据契约`。开始前先阅读 `c:\\Users\\Xcy24\\Desktop\\ai写作\\plan\\01-产品方案与PRD.md`、`02-技术架构方案.md`、`03-品牌与界面规范.md`、`05-UIUX设计规范.md`、`07-AI配置安全与长上下文方案.md`、`08-env变量设计与密钥托管规范.md`、`09-数据模型与接口契约初稿.md`、`plan\\list\\00-统一执行规范.md` 和本文件。当前阶段只允许本地服务器运行，禁止任何公网部署和生产环境配置。你只允许修改服务端、数据库、接口契约、共享类型与本地联调所需文件，不要写前端视觉页面。所有 AI 请求必须经本地后端统一代理，真实密钥只允许放服务端 `.env`，不得写进前端、示例响应、README 或日志。先把用户、小说、章节、评论、帖子、消息、AI 封面相关的数据模型和接口定义清楚，字段命名统一、可复用、可供前端并行开发。完成后输出接口清单、数据模型和本地启动说明。
