APP_NAME=启创墨域
APP_ENV=development
APP_PORT=3001
APP_WEB_URL=http://localhost:5173
APP_SERVER_URL=http://localhost:3001
DATABASE_URL=postgresql://<local_user>:<local_password>@localhost:5432/<local_database>?schema=public
AUTH_SESSION_SECRET=replace_with_a_local_random_secret
# AES-256-GCM master key for encrypted built-in/custom model API keys.
# Use an independent long random value in production; when omitted, AUTH_SESSION_SECRET is used for compatibility.
MODEL_CONFIG_ENCRYPTION_KEY=replace_with_an_independent_long_random_secret
AUTH_COOKIE_DOMAIN=
AUTH_COOKIE_SECURE=false

# Tencent Cloud SMS for login/register verification
SMS_TENCENT_SECRET_ID=replace_me
SMS_TENCENT_SECRET_KEY=replace_me
SMS_TENCENT_REGION=ap-guangzhou
SMS_TENCENT_SDK_APP_ID=replace_me
SMS_TENCENT_SIGN_NAME=replace_me
SMS_TENCENT_TEMPLATE_ID_AUTH=replace_me
SMS_CODE_LENGTH=6
SMS_CODE_EXPIRES_IN_SECONDS=300
SMS_CODE_COOLDOWN_SECONDS=60
SMS_CODE_HOURLY_LIMIT=5

VITE_APP_NAME=启创墨域
VITE_API_BASE_URL=

AI_TEXT_BASE_URL=https://api.deepseek.com
AI_TEXT_API_KEY=replace_me
AI_TEXT_MODEL=deepseek-v4-flash
# 单轮 LLM 调用最大输出 token（默认 8192 = deepseek-chat 上限；不配时 DeepSeek 默认仅 4096 会截断长章）
# AI_TEXT_MAX_OUTPUT_TOKENS=8192

# AI_AGENT_MODEL=deepseek-v4-flash
# 思考强度（DeepSeek 思考模式）：low/high/max，默认 high（兼顾周到与成本；low 考虑不周、max 过度思考）
# AI_REASONING_EFFORT=high
# 单次任务最大轮次（默认 100）与 token 预算片（默认 2000000）；预算可由作者显式上调，服务端 clamp 到硬顶（默认 5000000）
# AGENT_MAX_TURNS=100
# AGENT_RUN_TOKEN_BUDGET=2000000
# AGENT_RUN_TOKEN_BUDGET_CEILING=5000000
# Agent 上下文窗口安全默认值；内置/自定义模型可用 metadata.contextWindowTokens 单独覆盖
# AGENT_CONTEXT_WINDOW_TOKENS=128000
# 墙钟防护：总帽（默认 60 分钟，自动续跑后切长任务帽 180）+ 空转帽（默认 10 分钟）
# AGENT_RUN_WALL_CLOCK_MINUTES=60
# AGENT_RUN_WALL_CLOCK_LONG_MINUTES=180
# AGENT_RUN_IDLE_MINUTES=10
# 信道重复检测：observe=只记日志零干预（默认），enforce=提醒→再命中终止
# AGENT_REPEAT_GUARD_MODE=observe

AI_IMAGE_BASE_URL=https://your-image-provider.example.com/v1/images/generations
AI_IMAGE_API_KEY=replace_me
AI_IMAGE_MODEL=gpt-image-2

# 视觉推理旁路（Agent view_image 工具：智谱 GLM-4.1V-Thinking-Flash，OpenAI 兼容）
# 不配置时 view_image 工具回填「视觉服务未配置」观察，不阻塞 run
# AI_VISION_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# AI_VISION_API_KEY=
# AI_VISION_MODEL=glm-4.1v-thinking-flash
# AI_VISION_TIMEOUT_MS=60000
# AI_VISION_MAX_CONCURRENT=4
# 全权限开关：默认 true（'ask' 类动作自动批准）；false 回退审批流
# AGENT_AUTO_APPROVE=true

# 听书 TTS（Edge TTS 免密钥）：edge 启用 / disabled 全站隐藏听书入口
# TTS_PROVIDER=edge
# TTS_DEFAULT_VOICE=zh-CN-YunjianNeural
# TTS_CACHE_DIR=
# TTS_CACHE_MAX_MB=2048
# TTS_TIMEOUT_MS=30000

# Agent 联网搜索：auto = 有博查 key 用博查、否则免 key 搜狗抓取兜底、Bing 末位兜底；也可 disabled
# 另有 web_read 网页深读（无需配置，带 SSRF 防护与每任务 8 次预算）
WEB_SEARCH_PROVIDER=auto
# WEB_SEARCH_BOCHA_API_KEY=
WEB_SEARCH_TIMEOUT_MS=15000
# web_read 深读增强：readability 主线正文提取（false 一键回退正则提取）
# WEB_READ_USE_READABILITY=true

# Agent 2.0 独立功能开关（默认 true；false 可单项回退，不删除数据）
FEATURE_VOLUME_ENABLED=true
FEATURE_CHANGESET_ENABLED=true
FEATURE_MEMORY2_ENABLED=true
FEATURE_SKILL2_ENABLED=true
FEATURE_STORY_COMPILER_ENABLED=true
FEATURE_HUMANITY_QUALITY_ENABLED=true
FEATURE_CRAFT_LIBRARY_ENABLED=true
FEATURE_RESEARCH_DOSSIER_ENABLED=true
FEATURE_FEEDBACK_FLYWHEEL_ENABLED=true
FEATURE_SKILL_SHARING_ENABLED=true
FEATURE_DUAL_WORKSPACE_ENABLED=true
# 灰度用户 ID，英文逗号分隔；留空表示全量开放
# AGENT2_ROLLOUT_USER_IDS=
# 托管 Reader 爬虫兜底层（仅本地提取正文不足时触发）：off | jina | firecrawl
# r.jina.ai 境内可达性有争议（jina-ai/reader issue #1237），启用前在 VPS 实测：curl https://r.jina.ai/https://example.com
# firecrawl 为 Cloud 免费档（1000 页/月），需 key
# WEB_READER_FALLBACK=off
# JINA_READER_API_KEY=
# FIRECRAWL_API_KEY=

# Optional bootstrap owner for `npm run prisma:seed`
# SEED_OWNER_EMAIL=owner@example.com
# SEED_OWNER_PHONE=
# SEED_OWNER_NICKNAME=站点主理人
# SEED_OWNER_PASSWORD=replace_with_a_strong_password
# SEED_OWNER_ROLE=admin
# SEED_OWNER_BIO=
