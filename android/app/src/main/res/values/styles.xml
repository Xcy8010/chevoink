<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/splashBackground</item>
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <!-- 开屏使用指定的透明无字 Logo，与白底桌面图标分离。 -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/chevoink_splash_icon</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/chevoink_splash_icon</item>
        <!-- 图标圆形背景与纸色窗口同色：确保任何 OEM 默认圆背都是纸色（不可见） -->
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splashBackground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
